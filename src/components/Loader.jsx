import React from 'react';
import './Loader.css';

const Loader = () => {
  return (
    <div className="loader">
      {/* The loader animation is now applied via CSS */}
    </div>
  );
};

export default Loader;